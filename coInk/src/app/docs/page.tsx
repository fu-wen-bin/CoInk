'use client';

import DocsHomeView from '@/app/docs/_components/DocsHomeView';

/**
 * 文档应用主页：顶栏 + 快捷入口（模板功能已停用）+ 最近访问 / 归我所有 / 与我共享 / 收藏
 */
export default function DocsPage() {
  return <DocsHomeView />;
}
